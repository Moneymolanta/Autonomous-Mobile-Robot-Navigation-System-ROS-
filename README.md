# Autonomous-Mobile-Robot-Navigation-System-ROS-
In this project, we combined our work in several labs to have our robot plan and navigate a path through a maze. The TurtleBot localized itself in the maze (particle filter), planned a path to the goal (RRT), and followed that path (waypoint following).
